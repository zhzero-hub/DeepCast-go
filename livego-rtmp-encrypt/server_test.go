package livego

import (
	"testing"
)

func TestStartFfmpeg(t *testing.T) {
	//ch := make(chan int)
	//go StartFfmpeg("2560x1440", &ch)
	//time.Sleep(10 * time.Second)
	//ch <- 1
	//go StartFfmpeg("426x240", &ch)
	//select {}
}
